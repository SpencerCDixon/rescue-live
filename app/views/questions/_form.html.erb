<%= form_for @question do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.submit (f.object.new_record? ? "Create Question" : "Update Question"),
                                               class: "button tiny success" %>
<% end %>
